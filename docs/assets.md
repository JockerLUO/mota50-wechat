# 美术素材系统（v0.1）

本文说明 `assets/` 下的素材从哪来、怎么加工、以及运行时怎么用。
授权与逐包署名见 [`assets/ATTRIBUTION.md`](../assets/ATTRIBUTION.md)，
本文只讲工程结构与决策。

---

## 1. 目标与约束

| 目标 | 具体要求 |
|---|---|
| 风格 | 贴近马里奥 / **塞尔达织梦岛** —— 16×16 俯视像素，粗块、高饱和、深描边 |
| 权属 | **必须可商用**，且不触碰任天堂 IP（微信小游戏需软著登记） |
| 多角度 | 人物与怪物等运动物体要有**多方向、多帧**素材 |
| 体积 | 微信小游戏对首包体积敏感，图集要小 |
| 可维护 | 素材加工必须**可重跑**，不许手工修图 |

---

## 2. 目录结构

```
assets/
├── ATTRIBUTION.md          授权与逐包署名（合规凭据）
├── MANIFEST.json           ★ 实体 → 图集坐标的唯一事实来源（脚本生成，勿手改）
├── raw/                    原始素材，只读。按来源分包归档
│   ├── 0x72/               326 个语义化命名的逐帧 PNG + 完整图集
│   ├── armm-zeldalike/gfx/ 8 张：角色表、NPC 表、三套图块、物件、字体
│   ├── kenney/             tiny-dungeon / tiny-town / roguelike-* / ui-pack / mobile-controls
│   └── tiny-zelder-clone/  备用
├── atlas/                  ★ 运行时实际加载的图集（脚本生成，勿手改）
│   ├── terrain.png  512×49      4.6 KB
│   ├── actors.png   512×134    12.0 KB
│   ├── monsters.png 512×169    18.1 KB
│   └── items.png    512×99      3.5 KB
└── preview/                目视核对图（不参与运行）
    ├── atlas-*.png         各图集的放大对照
    └── mock-board.png      ★ 用 MANIFEST 拼的模拟棋盘
```

**图集总大小 ~38 KB。** 作为参考，这比一张普通的背景图还小，
微信小游戏的分包压力基本可以忽略。

---

## 3. 选了哪些包，为什么

选型顺序是「先用现成的，找不到才生成」。最终 6 个包里，
**0x72 承担大头，ArMM1998 承担多角度**：

| 需求 | 采用 | 理由 |
|---|---|---|
| 怪物（34 只，要动画） | **0x72** | 25 种生物 ×(idle 4 帧 + run 4 帧)，且**逐帧文件带语义命名**，不用目测切图 |
| 地形（墙/门/地面） | **0x72** | 全套墙体转角/侧边/顶边 + 8 种地面 + 门 + 梯子，是成套系统而非散件 |
| **勇者 / NPC 多角度** | **ArMM1998** | 全项目**唯一**提供「4 方向 × 4 帧走路 + 挥剑」的 CC0 来源 |
| 钥匙 / 盾牌 | **Kenney Tiny Dungeon** | 0x72 没有钥匙与盾，Kenney 有三色钥匙（#126 绿 / #127 红 / #128 蓝）与盾（#102） |
| 界面 / 触屏 | **Kenney** | 现成的 UI 包与虚拟摇杆，留给后续触屏与界面接入 |

### 被排除的候选

- **Ninja Adventure（CC0，内容极全）**：其 GitHub 仓库是 Godot 示例工程，
  **不含角色与怪物美术**（`content/character/` 只有一张 `Shadow.png`），
  完整包在 itch.io 但需交互式下载。**已排除**。
- **itch.io 直连**：测得需经代理，且多数包需要交互才能取到文件，不适合脚本化。
- **Liberated Pixel Cup**：多角度动画最全，但授权是 **CC-BY-SA 3.0 / GPLv3**
  （传染性），且素材为 64×64，与 16×16 基准不匹配。**未采用**。

---

## 4. 网络取用的实测结论

素材站点在国内的可达性差异极大，直接决定了取用路径。实测数据：

| 通道 | 实测速度 | 结论 |
|---|---|---|
| `kenney.nl` 直连 | ~24 KB/s | 可用，逐个拉 |
| `opengameart.org` 文件 CDN | **2.2 KB/s** | 只适合小文件（171 KB 约 80 秒） |
| `opengameart.org` 网页 | 首页 30 s 超时 | 网页几乎不可浏览 |
| `raw.githubusercontent.com` **直连** | 1.3 KB/s | ⚠️ 直连反而慢 |
| `gh-proxy.com` + GitHub | **94 KB/s** | ★ 快通道，比 OGA 快 40 倍 |
| `ghproxy.net` + GitHub | 19 KB/s | 备选 |
| `itch.io` 直连 | 超时 | 走 `127.0.0.1:7890` 代理可通（3.9 s） |

**结论**：GitHub 上的素材走 `gh-proxy.com` 是最优路径；
OGA 只用来取那一个 171 KB 的 ArMM1998 包。

---

## 5. 加工流水线

```
tools/build-assets.py            切图 / 调色板变换 / 归一化 / 打包 / 断言
        │
        ├──▶ assets/atlas/*.png        运行时图集
        ├──▶ assets/MANIFEST.json      坐标映射表
        └──▶ assets/preview/atlas-*.png 目视核对

tools/preview-board.py           读 MANIFEST，拼一张模拟棋盘
        └──▶ assets/preview/mock-board.png

assets/preview/                   目视核对（不参与运行）
        ├── atlas-*.png            四张图集的放大对照
        ├── mock-board.png         python 侧拼的模拟棋盘
        ├── before-after-palette.png  地面配色 / 宝石的前后对照（§7 ④⑤）
        └── in-game-*.png          浏览器里真实渲染的截图（棋盘/商人/战斗）
```

重跑：

```bash
npm run assets                    # 等价于 python3 tools/build-assets.py
python3 tools/preview-board.py    # 目视核对用的对照图
```

> 需要 Python 3.8+ 与 Pillow（`python3 -m pip install pillow`）。
> 用虚拟环境时把解释器路径传给 `npm run assets`：`PYTHON=<venv>/bin/python npm run assets`。

> `tools/build-assets.py` 里 `MONSTERS` / `ITEM_SRC` / `TERRAIN` 三张表是
> **唯一的映射事实来源**。要换素材或调颜色，改表后重跑，不要改 `atlas/`。

---

## 6. 四条关键设计决策

### 6.1 16px 绘制基准 + 超采样出图（×4）+ 缩小落屏

棋盘格子是 **32px**（`LAYOUT.board.cell`），手绘 / 程序化坐标都写在 **16×16 的
绘制网格**上；**出图时 Scale2x 连做两遍升到 64px 网格**（`SS = 4`），运行时按
`drawScale = 0.5` 画进格子。落屏的设计像素数不变（16 × 2 = 64 × 0.5），但一个
素材像素占的设备像素从 ~5.4 降到 ~1.35 —— 这是「画面更精细」的来源。

为什么是 Scale2x 而不是双线性：它按 4 邻域决定 2×2 块内的对角填充，
斜向阶梯被抹成真正的斜边、直边原样保留 —— **去锯齿但不插值**。
双线性会把硬边糊掉，NEAREST 则什么都不做。

⚠️ 超采样有**信息上限**：第三方位图只有 16×16 的信息，摊到 64 网格不会多出
细节，只把轮廓磨得更圆（连做两遍还会把 1px 直角磨成圆角）。所以
**大面积平铺的手绘地形（地板 / 墙 / 上下楼梯）不走超采样 —— 它们直接画在
64 网格上**，那才是真多出来的细节。详见 `ui-prototype.md` §20–§22。

两套放大目标的差别（踩过的坑，见 §6.4）：

| 类别 | 出图规则 | 为什么 |
|---|---|---|
| 地形 | 统一放大到 **64 网格**（`terrain_raster`） | 地形一律占一格、落屏 = cell |
| 角色 / 道具 | **源 ×SS**（`supersample`） | 落屏尺寸各不相同，统一到 64 会把小道具放大一倍 |
| 怪物（手绘 7 种形状） | **手绘 32 网格 → Scale2x 一遍到 64**（`_mon_out`） | 直接画在 32 网格（`MON_W`，细节 = 旧 16 网格的 4×），与地形同理是真·手绘细节；落屏仍由 `drawScale` 决定 |

「大家伙」落屏 **48px**（1.5 格），同样超采样到 64 网格、倍数 **0.75**。
它曾经不超采样（16 网格 ×3），理由是非整数倍会毛躁 —— 那是「倍数只能取整」
时代的约束；现在不跟的话，它反而成了全屏像素最粗的东西。

### 6.2 怪物靠「调色板变体」成族，而不是逐个重画

34 只怪物的名单本身就成族：

```
skeleton / skeletonSoldier / skeletonCaptain      ← 骨 → 铁甲 → 金甲
juniorMage / seniorMage / juniorWizard / seniorWizard
juniorGuard / midGuard / seniorGuard              ← 青铜 → 白银 → 黄金
greenSlime / redSlime / bigSlime / slimeKing
orc / orcWarrior ·  bat / bigBat / vampireBat ·  knight / knightCaptain / darkKnight
```

**原版 50 层魔塔就是这么做的** —— 同图换色表达三级进阶。这不是偷懒，
是这个品类的既定视觉语言。所以 34 只怪物实际只需要 ~20 个基础造型。

变换分两种，**必须按材质选**：

| 函数 | 适用 | 理由 |
|---|---|---|
| `ramp(im, dark, light)` | 金属 / 骨甲（战士、骑士、骷髅、守卫、石巨人） | **保留明暗结构**，只换材质色。用转色相会把骑士糊成一坨纯色 |
| `hue_shift(im, deg, sat, val)` | 史莱姆、蝙蝠、法师袍 | 这些本来就是单色生物，转色相最自然 |

> ⚠️ **这条规则在 2026-09-22 被部分推翻**：上面的变体只解决「同族进阶」，
> 不解决「名字与素材根本对不上」—— 0x72 包里没有蝙蝠 / 龙 / 乌贼 / 石头人 / 史莱姆，
> 当初是靠 `hue_shift` 把 `swampy`（绿衣人）、`imp`（小恶魔）、`chort`（小鬼）
> 推成这些名字的，画面上是「标着『绿史』却是个拿铲子的绿衣人」。
> 现在这 13 只改成**按名称手绘**，见 §13。
> `hue_shift` 仍用于它擅长的场合（单色生物的同族换色）。

### 6.3 角色精灵「底部居中」对齐

所有角色类素材统一贴到一个底部居中的画布上（`bottom_center()`）。
俯视游戏里精灵必须**脚踩格子下沿**才站得住，居中的话会像浮在半空。
在加工阶段一次性对齐，渲染层就不必为每张图单独调偏移。

> ⚠️ **已发现但暂未修的缺陷：`Image.paste(im, pos, im)` 会平方 alpha。**
> 三参数的 `paste` 是「用 mask 覆盖」，实测把 alpha 从 102 变成 **41**
> （RGB 也被压暗：`200 → 157`、`(42,43,53) → (17,17,21)`）。
> 而本流水线里这个动作发生了**两次**（`bottom_center()` 一次、图集装配一次），
> 于是勇者脚底那层源 alpha=102 的影子被平方两次 → **7**，等于画了个看不见的影子。
> 正确写法是 `alpha_composite`（对空画布就是逐像素拷贝，实测 102 → 102）。
>
> **为什么先不修**：修好后勇者会长出影子，而 NPC 还没有影子，
> 两只并排会不协调；要一起补就得先定 NPC 的影子画法。
> 所以先把结论钉在 `build-assets.py` 的 `SOLID_ALPHA` 注释里，等补影子那轮一起做。
>
> **它有一个必须记住的副作用**：量精灵比例时**不能用「非透明包围盒」** ——
> 那两行 alpha=7 的残影会被算进去，勇者会被量成 22~23 而不是 20。
> 所有比例判据改用实心阈值 `SOLID_ALPHA = 250`（描边是 255、半透明影子 ≤178），
> 好处是**将来把 paste 修好了，这些判据也不会变**。详见 `ui-prototype.md` §17.2 / §18.5。

### 6.4 归一化：图集里的怪物帧一律 64×64 出图、绘制在 32 网格上

手绘怪物现在直接画在 **32×32 的绘制网格**（`MON_W = MON_H = 32`，是旧 16 网格的
4 倍像素密度）上，再经 `_mon_out()` Scale2x 一遍升到 **64×64 出图网格**（`RASTER_TILE`）。
落屏尺寸只由 `drawScale` 决定（16 × 2 = 64 × 0.5，与地形同构）。

> 0x72 里 `ogre` / `big_demon` / `big_zombie` 是 **32×32 源**，曾因混入被 `drawScale=3`
> 算成 96px（3 格）。现在手绘怪一律 32 网格 → Scale2x 到 64，与「源 ×SS」等价，
> 不再有「32 源直接混入」的坑；倍数同步除掉 SS，落屏设计像素数不变。

> ⚠️ 地形的出图规则与这里是**两套**：地形一律放大到 64 网格，而
> 角色 / 怪物 / 道具按「源 ×SS」。门的源本来就 32×32，按 ×SS 会到 128，
> 落屏 64px —— 一扇门顶两格宽（本轮实际踩到）。详见 §6.1 的表。

---

## 7. 构建脚本里的自动断言

配色和一致性不能靠「看着还行」。`build-assets.py` 内置了若干组断言，
失败会出现在输出的「需注意」列表里。
下面按「人类读得懂的顺序」编号，括号里给出**脚本里的落点**（脚本内部的
注释编号与本节可能不完全一致，以函数名为准）：

**① 地形色彩区间**（`verify_terrain`）

| 地形 | 色相要求 | 明度下限 |
|---|---|---|
| 空地 | 0–48°（暖砂石） | **0.55** |
| 岩浆 | 8–48°（橙红） | 0.30 |
| 黄门 | 32–62° | 0.35 |
| 蓝门 | 190–250° | 0.35 |
| 红门 | 335–360° 或 0–22°（红跨 0°） | 0.35 |

> 这组断言是**踩坑换来的**。最初红门用 `hue_shift(+250°)` 实现，
> 肉眼在缩略图上以为是「转了色」，实测平均色相落在 **250.8°（蓝紫）——
> 门是紫的**；岩浆被转到 **201.6°（青色）**。
> 用色相区间一测就现形。

**② 假墙 ≡ 真墙（逐像素）**（`verify_terrain`）

假墙一旦看得出来，这个地形的**玩法就直接失效**。所以构建时断言
两者图像完全一致 —— 这不是审美问题，是正确性问题。

**③ 上 / 下楼梯必须是两种形体，不能互为翻转**（`verify_terrain`）

这条是**用户直接报上来的**：塔里上楼和下楼原来都用 `floor_ladder`
（下＝原图、上＝垂直翻转），而 `floor_ladder` 近乎上下对称，
翻转后肉眼读不出区别，**玩家分不清哪一格是上、哪一格是下**。

只判「两张是否逐像素相同」是不够的 —— 上面那种翻转写法能轻松通过。
现在判**四件事**，缺一不可：

| # | 判据 | 抓的是什么 |
|---|---|---|
| 1 | `up` 与 `down` 逐像素不同 | 根本没换图 |
| 2 | `up` ≠ `flip_vertical(down)` | 「翻转当区分」这种假修复 |
| 3 | `down` 横剖面自左向右**单调变暗**（落差 ≥ 0.15，无回弹 > 0.02） | 下沉梯段的形体：越往右下越深 |
| 4 | `up` 横剖面自左向右**单调变亮**（落差 ≥ 0.15，无回退 > 0.02） | 上升梯段的形体：越往右上越接近出口 |

判据 3/4 是关键：**只改明暗不改形体的写法会让它们挂掉**，反之亦然。

两张图是**同一族画法的两个方向**（都手绘在 64 网格上）：
`down` 是四级下沉阶梯（`_stairs_down()`，`STAIR_TREAD` 倒序 + `STAIR_WELL`
逐级压暗、井底近黑），`up` 是四级上升阶梯（`_stairs_up()`，`STAIR_TREAD`
正序 + 梯顶出口光）。实测 `down` 剖面 0.41 → 0.18（沉）、
`up` 剖面 0.21 → 0.64（升），走向相反，互不相等也不互为翻转。

> 上一版 `down` 画的是**同心方井**（8 个方环向内变暗，判据是「谷」）。
> 用户反馈那一圈方形读不出楼梯、像个坑，而且和侧视的上楼梯**不像一套** ——
> 于是改成同族反向。判据也跟着从「谷」换成「递减」：判据必须描述
> **当前这一版的形体**，否则它会保护一个已经被换掉的画法。

配套还有一条**文本断言**：`MANIFEST.json` 里这两个瓦片的 `src`
**不允许出现「翻转」二字**。数值判据能被绕过，文字溯源不能 ——
将来有人把 `src` 改回「floor_ladder 翻转」，构建会直接报出来。

> 两条素材是怎么画的、判据的完整推导、以及运行时那条对应的断言 A16，
> 见 `ui-prototype.md` §19.5 / §19.6。

**④ 「大家伙」落屏尺寸一致**

防止体型层级倒挂。这条也来自真实翻车：`big_demon` 原本 32×32，
按 1:1 绘制只有 32px，而 16×16 放大 3 倍的巨龙有 48px ——
**魔王看着比小龙还弱**。

**⑤ 地面 / 墙的明度比 ≥ 2.0**（`verify_terrain`）

这是后面接入渲染层时才暴露的一条：0x72 的 `floor_1` 与 `wall_mid`
**共用同一套三色** `(72,59,58)/(119,92,85)/(34,34,34)`，源素材根本没打算
靠颜色区分地面和墙。只给地面提亮 ×1.5 时，两者**平均亮度 0.364 vs 0.232，
比值 1.57** —— 迷宫的地面/墙读不出来，整屏糊成一坨褐色；
更糟的是墙的砖缝 `(34,34,34)` 正好等于史莱姆身体的深色，怪物会「粘」在墙上。

改成 `ramp_norm()`（先把亮度拉伸到满量程再映射到暖砂石渐变）后
**0.643 vs 0.232，比值 2.77**。断言卡在 2.0，防止这件事回潮。

> 地板后来改成**手绘**（`_floor()`，见 §6.1 / `ui-prototype.md` §21）：
> 它占屏面积最大，而第三方位图只有 16×16 的信息，超采样摊细它并不会多出
> 细节。手绘的配色同样压在暖砂石族里，这条明度比断言**原样适用** ——
> 断言描述的是约束（地面必须远亮于墙），不是某个具体画法，所以换画法时
> 它不用改。实测手绘版仍稳定在 2.7 上下。
>
> 墙随后也改成了**手绘**（`_wall()`，见 `ui-prototype.md` §22）：砖身
> `(78,63,60)`、缝 `(38,32,31)` 仍留在 0x72 的墙色族里（灰缝只是比原来的
> `(34,34,34)` 略偏暖 —— 那个值正好等于史莱姆的深色，怪物会「粘」在墙上）。
> 实测平均亮度 0.631 vs 0.219，比值 **2.88**，断言继续原样生效。

> 教训：这类问题在缩略图上**非常容易「看着还行」地蒙混过去**，
> 只有数值能抓住。凡是「靠视觉区分」的东西，都应该有一条数值断言。

**⑥ 宝石必须是菱形且颜色正确**（`verify_items`）

失败模式同样是**静默**的。上一版从 0x72 图集 `r12c1`/`r12c3` 切了两块
**纯色矩形**当红/蓝宝石（那两格其实是图集里给别的用途画的色块），
构建不报错、渲染也不报错，只是棋盘上出现两个莫名其妙的正方形。

现在两条断言一起上：**行宽轮廓**必须是菱形（中间最宽、上下收窄），
**主色相**必须红 ≈0°、蓝 ≈210°。色相用 `dominant_hue()` —— 取「有彩度像素的
颜色众数」而不是均值：像素画的颜色是离散的，均值会把红色（5°）和描边墨色
（312°）平均成 150° 附近的中间色，让判断假通过。

**⑦ 墙必须是手绘错缝砌法**（`verify_terrain` 构建期 + 运行时 A19）

墙也改手绘之后，需要一条判据防止它**悄悄退回第三方位图**（那样密度断言
还绿着，但细节没了）。运行时 A19 直接量发布出去的图集帧：

| # | 判据 | 抓的是什么 |
|---|---|---|
| 1 | 调色板 ≥ 5 色 | 0x72 位图恰好 3 色，超采样不会多出颜色 —— 这条单独就能证明「不是那张位图」 |
| 2 | 每层砌层都有缝列（显著暗于砖身的整列） | 缝没了 = 砌法没了，墙读成一块平板 |
| 3 | 第 0/2 层缝列**同相**、第 0/1 层**不同** | running bond 的定义；错缝也是平铺不露接缝的前提 |
| 4 | MANIFEST 的 `src` 含「手绘」 | 改了脚本没重跑 assets |

构建期同源的约束：假墙(11) 与真墙(1) 调用**同一个函数**（不只是同一张图），
逐像素一致由 ② 保证；压顶（`_wall_cap`）必须吃掉第一层与第二层之间的整道
横缝，否则压顶底下会留下 2px 孤零零的缝。

---

## 8. MANIFEST.json 结构

```jsonc
{
  "meta": { "baseTile": 16, "rasterTile": 64, "supersample": 4, "cell": 32,
            "drawScale": 0.5, "bigScale": 3, "atlases": { … } },
  // rasterTile = baseTile × supersample；drawScale = cell / rasterTile

  "terrain": {
    "0":  { "atlas": "terrain", "x": 0,  "y": 0, "w": 64, "h": 64, "drawScale": 0.5,
            "name": "floor", "src": "手绘 · 错缝石板（直接画在出图网格上）" },
    "1":  { …, "name": "wall", "src": "手绘 · 错缝砌法（直接画在出图网格上）" },
    "1:top": { … },   // 上方无墙时的「顶边」变体：手绘墙身 + 手绘石质压顶
    "11": { … }       // 假墙：与 "1" 逐像素相同（同一个函数生成）
  },

  "actors": {
    "hero": {
      "src": "ArMM1998 / Zelda-like — character.png",
      "dirOrder": ["down", "left", "up", "right"],
      "walk":   { "down": [ {x,y,w,h,atlas,drawScale} ×4 ], "left": […], … },
      "attack": { … }
    },
    "npcs": { "sage": { "walk": { "down": [ …×4 ], … } }, "merchant": { … } }
  },

  "monsters": {
    "skeleton": {
      "src": "0x72/skelet", "note": "骨色原样", "drawScale": 1,
      "frame": { "w": 32, "h": 32 },
      "idle": [ {x,y,w,h,atlas} ×4 ], "run": [ … ×4 ]
    },
    "未映射的怪": null              // 明确标 null，渲染层据此回退程序化图形
  },

  "items": { "redPotion": { "atlas": "items", "x": …, "src": "0x72/flask_red" } }
}
```

### 动画约定

- 走路是 **4 帧循环**，可直接按 `0→1→2→3` 顺序播放。实测每个方向
  4 帧中**有 3 帧互不相同**，即「3 姿势 + 1 回位」的经典循环结构。
- 每个朝向的帧**互不相同、互不为镜像**（已用逐像素比对确认），
  所以 4 个方向必须各自取图，不能靠水平翻转省素材。

---

## 9. 覆盖情况

| 类别 | 覆盖 |
|---|---|
| 地形 | **13/13**（12 种地形 + 1 个墙顶边变体） |
| 怪物 | **34/34** 数据中的怪物全部有素材（另有 2 只备用：goblin、ice_zombie） |
| 道具 | **32/32** |
| 勇者 | 4 向 × 4 帧走路 + **4 向 × 4 帧挥剑** |
| NPC | **6 人 × 4 向 × 4 帧**（16×26 程序化像素画，**本项目自绘** —— 见 §9.1） |

**零缺口。** 渲染层目前不需要走程序化兜底。

### 9.1 NPC 为什么从「取素材」改成了「自己画」

第三方免费素材包里**只有一个 NPC 角色**（`NPC_test.png`）。最初六个职能
（智者 / 商人 / 商店 / 小偷 / 仙子 / 公主）共用这一张图 —— 结果是「不同功能的
NPC 长得一模一样」，地图上根本分不出谁是卖东西的、谁是给情报的。

素材库里也没有第二套可用的多角色 NPC 集，于是改为在
`tools/build-assets.py` 里用 `_put()` 一个原语手绘五行像素块。六个职能各一组
造型参数（`NPC_ART`），靠**剪影 + 帽子 + 手持物**区分：

| 职能 | 剪影记号 | 手持物 |
|---|---|---|
| `sage` 指引 | 灰白长袍 + 尖顶软帽 + 白须 | 法杖（顶端宝石） |
| `merchant` 交易 | 暖褐短袍 + 宽檐帽 | 腰侧钱袋 |
| `shop` 属性 | 深绿外袍 + 皮围裙 | 手边一摞金币 |
| `thief` 情报 | 兜帽 + 蒙面（只留眼缝） | 腰侧短匕 |
| `fairy` 祝福 | 青白长裙 + 背后薄翅 | 星杖 |
| `princess` 主线 | 粉裙 + 长发 + 金冠 | —— |

**两轮反馈把「造型」这件事拆成了两个可量的维度**（细节见 `ui-prototype.md`
§17.2 与 §18）：

| 轮次 | 反馈 | 修的是 | 判据 |
|---|---|---|---|
| 第一轮 | 「NPC 模型改小一点，比例适中」 | **高度** | 实心行区间 == 勇者，高 20 |
| 第二轮 | 「头上长方形太细，身体太宽」 | **头身比** | 头宽 ≥ 10 且 下摆 ≤ 头 + 2 |

于是横向解剖也收进常量表（`NPC_FACE_X/W`、`NPC_TORSO_X/W`、`NPC_ARM_X/W`、
`NPC_ROBE_X/W`），原则两条：**头不比身体窄**、**肩要比下摆宽**。

> 一个必须记住的实现约束：`add_outline` 是 **1px 八邻域膨胀**，
> 所以相邻两段的**绘制**宽度至少要差 **2**，只差 1 会被描边抹平。
> 第一版重画把头和肩都画 10 宽，结果整只精灵在 12 宽上从头顶平到脚 ——
> **变成一根柱子**。逐行数字全对，看整体才不对。

生产端的断言（`verify_npc_art()` + `verify_npc_scale()`，跑在构建里，
不看图也能拦住回归）：

1. **六个剪影必须互不相同** —— 用 alpha 通道逐像素比对。这条针对的正是
   「改回共用一张图」这种退化（那种情况下画面照旧，只是又变回六个人一个样）。
2. **脚必须踩在解剖表那一行**（`NPC_FOOT_ROW + 1 = 23`，与勇者的脚底同高）——
   精灵是底部锚定的（`anchor.set(0.5, 1)`），脚没有确定的落点就会出现
   「一只脚踩地、一只脚悬空」这种只在画面上看得见的错。
   > 期望值**不是「帧的最后一行为空就报错」**：帧高 26，勇者脚底还带 2 行半透明影子
   > （见 §6.3）。所以钉的是解剖表，不是帧底。
3. **头身比**：头宽 ≥ 10（脸 8 + 左右各 1 描边）、下摆宽 ≤ 头 + 2、最宽行 ≤ 15，
   且静止帧的实心行区间必须**恰好等于勇者朝下那帧**（`4..23`）。
4. **呼吸帧只抬上身**：底行不变（脚不离地），顶行不越过勇者的最顶行。

> 宽度取的是「**含画布中心的那一段**实心像素」，不是「最左到最右的跨度」——
> 手持物画在身体右侧，描边后会和身体连成一片，跨度会把道具算进「身体有多宽」，
> 而「身体太宽」恰恰是这条断言要抓的东西，量法不能自己把它放大。

配色与 `src/render/theme.ts` 的 `NPC_ROLE` 一一对应，同一职能在整座塔里颜色恒定；
渲染层另有 4 帧呼吸动画（头与躯干上移 1px、下摆与脚不动）。

> ⚠️ **6 套造型里有 2 套在游戏里见不到**：`thief`(34) 与 `fairy`(35) 这两个职能
> 在参考源的地图里**从未被放置**（role 层只用了 32/33/36/39）。造型、职能色、
> 台词都已就绪，缺的是地图数据 —— 这是参考源自身的不完整，
> `npm run validate` 的 I 段每次都会报告，详见 `docs/known-gaps.md`。

---

## 10. 关于「生成」的比例

用户的要求是「优先用现成的，没有合适的再生成」。实际执行结果：

| 来源 | 数量 | 说明 |
|---|---|---|
| 0x72 直接取用 | 10 项道具 + 全部地形 + 23/36 只怪物 | 药水、剑、金币堆 |
| Kenney 取用 | 9 项 | 三色钥匙、五级盾牌 |
| 0x72 图集内切取 | 1 项 | 镐（`frames/` 里没有） |
| **程序化生成** | **12 项** | 见下 |
| **合计** | **32 项，生成占比 37.5%** | |

生成的 12 项：炸弹、十字架、雪球、地震卷轴、镜翼、上飞翼、下飞翼、
传送阵、怪物图鉴、笔记本、**红宝石、蓝宝石** —— 这些在六个免费包里
**确实找不到对等物件**。由 `build-assets.py` 的 `gen_icon()` 绘制，
每张都过一道 `add_outline()`，保证和手绘素材一样带深色描边，
混在一起不会露馅。

> 宝石原本试图从 0x72 图集切（`frames/` 里确实没有），但切到的是两块
> **纯色矩形**，棋盘上就是两个方块。核对过 0x72 全部 130 张帧
> （只有 `skull` / `coin` / `flask_*` / `ui_heart_*`）和 Kenney 的
> `roguelike-rpg-pack` / `tiny-dungeon`（没有红/蓝菱形）之后，
> 才确认这是**真的缺**，于是生成。见 §7 第 ⑤ 条断言。

**怪物：13 只手绘 / 23 只取素材。** 全塔 36 只里，23 只由 0x72 的角色底图
按调色板变换得到（`ramp` 保留明暗结构 / `hue_shift` 换色）；另外 13 只
**名字与素材根本对不上**，已改成按名称手绘（史莱姆族 4、蝙蝠族 3、
石人 / 乌贼 / 龙 / 吸血鬼 / 魔王 2），见 §13。

> **一处诚实说明**：AI 图像生成（文生图）**无法可靠产出多方向走路循环**——
> 它做不到同一角色在 4 个朝向间保持造型一致，更做不到帧间连贯。
> 所以「多角度角色」这件事只能靠现成素材解决，这也是本次把
> ArMM1998 的角色表当作关键路径的原因。生成只用在单帧图标这类
> 不涉及方向与连贯性的地方。

---

## 11. 接入渲染层（已完成）

图集已经真正跑在游戏里了。落点如下：

| 文件 | 改动 |
|---|---|
| `src/render/atlas.ts`（新增） | 加载 4 张图集 + 按 `MANIFEST.json` 建 `Texture` 索引；每个 `source.scaleMode = 'nearest'`、`addressMode = 'clamp-to-edge'` |
| `src/render/board.ts` | 121 个地形槽位改用 `Sprite`（墙按邻域选 `1` / `1:top`）+ 16px 地面平铺底层；怪物走 `idle` 循环精灵；勇者按朝向切 `walk` 帧、战斗时播 `attack` 帧；NPC 用 4 向精灵 |
| `src/render/hud.ts` | 钥匙与道具栏图标改用 `items` 贴图 |
| `src/render/trade.ts` | 商人 / 商店条目图标改用 `items` 贴图，买不起时置灰（去掉 tint） |
| `src/app.ts` | `create()` 里 `await loadAtlas()`（Board 构造时要读 `atlas.ready`）；战斗触发时 `playHeroAttack()` |

**两种缩放策略，不是一个**

- **怪物 / 角色 → 按 `drawScale`**（常规 64 网格 ×0.5、BOSS 64 网格 ×0.75）。32–52px 这个尺度上，
  非整数倍会让像素宽窄不一，肉眼可见。
- **道具 / 图标 → `fitSize()` 等比适配**。小图标上的非整数缩放看不出来，
  而等比是必须的 —— 拉成正方形会把剑压扁。

**保留 `icons.ts` 作为兜底**：图集加载失败时 `atlas.ready === false`，
全部退回程序化矢量图形。所以「图集挂了」只会变丑，不会白屏。
加载本身也包在 `try/catch` 里，不抛。

**验证方式**：`atlasverify.mjs`（Playwright + 本地 Chromium）从
`window.mota.game` 读**场景图**而不是只看截图 —— 断言 121 个地形槽位、
76 个像素精灵全部 `scaleMode === 'nearest'`、勇者走路帧 32×52 / 挥剑帧 40×52（构建期已超采样）、
控制台零错误，并逐层打开商人 / 商店 / 战斗面板取图。

---

## 12. 塔壁平铺地图内墙贴图（同源，不是调色）

棋盘外围那圈「塔壁」原本是一组**自绘的冷灰蓝**（`#8b98ac`），而地图内部是暖褐石墙
（墙贴图 `#433836`，亮部 `#775c55`）—— 冷暖两套色系撞在一起，一眼就看出「周边是后期贴上去的」。

调色对齐是治不住的（两套色系的中间值是脏的），所以改成**平铺同一张贴图**：
`board.ts` 的 `buildParapet()` 直接取 `atlas.terrain('1')` 的 `source.uid`
—— 与地图内墙**同一个来源**，结构上就不可能不一致。

| 手法 | 为什么 |
|---|---|
| 平铺地图内墙贴图 | 同源 → 色相 / 砖缝 / 亮暗关系全部自动一致，换素材也不会漂 |
| `Container.mask`（Graphics 圆角矩形）裁切 | 平铺是矩形铺满，塔壁要圆角；遮罩裁出圆角 |
| 城垛与外缘描边**画在 `wall` 容器之外** | 否则会被遮罩一起裁平（这是本轮踩到的实际坑） |
| 城垛凹口**不画** | 让星空从缺口透出来，是「位面可见」的一处细节 |

`theme.ts` 另留一组 `STONE` 石材令牌，取自那张贴图的实际像素色，
给**不贴图也要有颜色**的地方用（棋盘底板、塔壁内缘阴影、外缘描边）：

| 令牌 | 值 | 取自贴图哪里 |
|---|---|---|
| `face` | `#7a5c4e` | 墙身主体（亮部 `#775c55` 往暖里推一档） |
| `faceLit` | `#9a7a68` | 受光面（城垛顶、砖面上沿） |
| `faceDark` | `#503c34` | 背光面（砖面下沿、石基）—— 棋盘底板用它 |
| `joint` | `#2a201c` | 砖缝 |
| `top` | `#b08e78` | 城垛顶面（朝天，最亮） |
| `inner` | `#1f1815` | 贴着棋盘那一圈的内阴影：让地图**嵌**进塔壁里，而不是浮在墙上 |
| `edge` | `#120d0b` | 塔壁外缘描边（与夜色交界） |

**回归保护（`verify-visual.cjs` 的 A9）**：断言 `__wallSources()` 里
`parapet === inner`（比 `source.uid`，不是比颜色）。比 uid 的好处是
「以后换贴图 / 又加一层自绘颜色叠加」也会立刻红 —— 颜色可以蒙对，uid 蒙不过去。
另有像素核对：截图里塔壁与地图内墙的色相差实测 **15°**、`R > B`（暖色）。

> 素材侧无需任何改动：塔壁用的是**已有的**地形墙贴图，没有新增图片，
> 也没有进 `MANIFEST.json`。改的只是「谁在用它」。

---

## 13. 按名称程序化重绘：13 只与名字不符的怪物

起因是一句反馈：**「怪物模型优化要更符合怪物名称」**。

### 13.1 为什么换素材解决不了

先做的是**调查能不能换素材**，结论是不能：

0x72 地牢包一共 24 张角色底图（`skelet` / `knight` / `imp` / `swampy` / `chort` /
`ogre` / `wizzard_*` / `orc_*` / `big_demon` …），**全部都是人形**。
包里**没有**蝙蝠、龙、乌贼、石头人、史莱姆、吸血鬼。
备选包（Kenney `roguelike-characters` 等）也只有人形与四足动物，没有能塞进
16px 像素画风格的非人形。

于是第一版只能「硬凑」——用一个人形去顶一个不是人的名字：

| 怪物 | 旧素材（0x72） | 问题 | 现在 |
|---|---|---|---|
| `bat` / `bigBat` / `vampireBat` | `imp`（有角的小恶魔） | 是小恶魔，不是蝙蝠 | 尖耳 + 展开的膜翼（窄翼 / 宽翼 / 宽翼 + 獠牙） |
| `greenSlime` / `redSlime` / `bigSlime` / `slimeKing` | `swampy` / `muddy`（穿绿衣的小人） | **「绿史」是个拿铲子的绿衣人** | 圆顶果冻；「大」用体量表达、「王」加三尖金冠 |
| `stoneGolem` | `ogre`（食人魔） | 是个胖人形 | 方块躯干 + 砖缝 + 发光眼，全程只有直角 |
| `kraken` | `swampy`（绿衣人形） | 完全不搭 | 宽圆头 + 侧鳍 + 六条**向外张开**的腕 |
| `dragon` | `chort`（小鬼） | 拿小鬼当魔龙 | **侧视**：角 + 长吻 + 背上的翼 + 尾 + 两条腿 |
| `vampire` | `zombie` 换紫 | 人形，但毫无吸血鬼特征 | 高领斗篷 + 獠牙 + 红眼 |
| `demonKing` / `demonKingTrue` | `big_demon`（32×32 归一化到 16） | 归一化丢像素，糊 | 巨角 + 膜翼 + 发光眼；真身换猩红 + 金冠 |

`MONSTERS` 表里**源名写 `"gen"`** 就是「这只不取 0x72，按名称手绘」。
最终：**36 只 = 23 只取素材 + 13 只手绘（7 种形状）**。

### 13.2 按名字对照 DQ 原型：七种形状各押一个识别记号

照搬 §9.1 的 NPC 做法（`_put()` 一个原语填像素块，硬边、不是矢量缩放），
全部七种形状只用 `_put` / `_sym` / `_ell` 程序化生成。**「像不像」押在每个
DQ 原型最招牌的一个记号上**，而不是面面俱到 —— 32 网格画不下第二个记号：

| 形状 | 用在哪 | 押的记号（对照 DQ 原型） |
|---|---|---|
| `slime` | 史莱姆族 ×4 | 水滴身 + 大白眼黑瞳 + 微笑 —— DQ 史莱姆的招牌就这一张脸（`top` 控大小、`crown` 控王冠） |
| `bat` | 蝙蝠族 ×3 | **圆球身 + 大眼 + 头顶呆毛 + 小圆翅** —— DQ 德拉基的关键词是「圆」，不是「翼展」（上一版押翼展，画成一团方块） |
| `dragon` | 魔龙 | 侧视朝左：长吻 + 双角 + 背脊翼 + 箭尖尾 + 两腿（四个记号分居四个方向） |
| `kraken` | 大乌贼 | **头占 2/3 + 触手短而弯、末端卷钩** —— 「头大触手短」，长直腕会读成外星人 |
| `golem` | 石头人 | 全程直角：方块头/躯干/砖缝 + 发光眼（DQ 巨像兵就是会走的石堆） |
| `vampire` | 吸血鬼 | **竖起的高领尖角 + 白脸**（强对比）+ 双手高举 —— DQ 吸血鬼的招牌姿势 |
| `demon` | 魔王 ×2 | 巨角 + 膜翼 + 发光眼（`_sym()` 镜像保证不差 1px）；真身加王冠 |

形状与颜色分开：`PROC_MONSTERS` 里只写颜色，形状函数不关心自己代表谁。
史莱姆族四只是同一个形状、四套颜色（绿 / 红 / 更大 / 金冠）。

> 2026-09-22 把怪物绘制网格从 16 提到 **32**（`MON_W = MON_H = 32`）：手绘细节密度
> 翻 4 倍（每网格像素从 ~5.4 设备像素降到 ~1.35），是「更精细」的直接来源；
> 出图仍经 `_mon_out` 升到 64 网格，落屏尺寸不变。呼吸帧 `_mon_squash` 的
> 上抬量随之从 1 调到 2（1px 在 32 网格上会被 Scale2x 吃掉看不见）。
>
> 同日第二轮按 DQ 原型返工了三只：**bat 族**（放弃大张膜翼，改德拉基的圆球+呆毛）、
> **kraken**（触手从画布底收短到头直径的 1.5 倍、末端加卷钩）、**vampire**
> （领子加高到脸的一倍、白脸放大、双手移到领尖 —— 胸口两颗白点会读成纽扣）。
> 另有一条小教训：**深色椭圆当「右下阴影」在高对比身体上会读成黑斑**
> （蝙蝠脸上多出一个「大鼻孔」），最后直接删掉，只留左上高光。

### 13.3 一条必须先记住的约束：`add_outline` 是 1px 八邻域膨胀

生成完一律过一道 `add_outline()` 补深色描边（和素材包里的原件一致）。
但它是**膨胀**，这带来一个反直觉的后果：

> **1~2 列宽的缺口会被描边填满。** 想把两块真正分开，缝至少要留 3 列
> （缝会被左右各吃掉 1 列）。

所以「腿之间留几列」「翼和身体怎么分开」不是审美问题，是被这条规则卡死的。
反过来利用它：**想要一条可见的分缝线，就故意留 1~2 列缺口** ——
描边会把它变成一条深色线，正好当「翼指分叉」。

### 13.4 教训：手写坐标会把形状藏起来（蝙蝠画成了「锚」）

第一版 `_mon_bat` 把翼写成「每行一段 `(x, y, w)`」，八行都写成了满宽。
渲染出来逐行量宽度是：

```
6 6 12 14 16 16 16 16 16 16 4 4 6 6 6     ← 旧版：8 行满幅 + 4px 细杆
```

宽横梁 + 细杆 + 底座 —— **这是一个「锚」**。

最要命的地方不是画错了，而是**看不出来**：坐标是数字，读代码读不出形状，
而当时看不了图，于是这版差点就这么交出去。

修法不只是改数字，而是**换表现方式**：把「画出来但看不出来的形状」改成
**可量化的剪影**。当前七种形状全部用 `_put` / `_sym` / `_ell` 程序化生成，
半径、张角、椭圆本来就是算法，在 **32 网格**上写起来比字符模板更准也更易复查
（模板在 32 网格下反而因为行列太多而难维护，故弃用 `BAT_*` / `DRAGON_ROWS`）。

关键的是**用宽度序列验证形状**，而不是「看图」：

```text
旧版蝙蝠的逐行宽：6 6 12 14 16 16 16 16 16 16 4 4 6 6 6   ← 锚（8 行满幅 + 细杆）
新版蝙蝠的逐行宽：8 8 10 12 16 16 16 16 14 12 10 8 6 8 8   ← 腰鼓（窄→宽→窄）
```

`_stamp` 当初对**行列长度**做断言，「多写少写一个点」在构建时就报错；现在等价地
由 `verify_mon_art` 的剪影/底行断言兜底（见 §13.7）。
蝙蝠的剪影从此是「窄 → 宽 → 窄」的**腰鼓**：

```
6 6 10 12 16 16 16 16 14 12 10 6 6 8 8 8
```

### 13.5 教训二：翼 / 尾 / 腿不能共用一个颜色

旧版 `dragon` 的翼膜、尾巴、腿全用 `dark`，结果右侧与下方连成一整团黑红 ——
**剪影退化成一整块砖**。现在拆成四个色：`body`（红身）/ `belly`（亮腹甲）/
`wing`（暗栗膜）/ `dark`（近黑腿与尾），四个块才分得开。

### 13.6 怎么看：量剪影，别「看图」

看不了图的时候（这次的处境），唯一可靠的验证是把每个形状的 **alpha 通道导成
ASCII 廓形 + 逐行宽度序列**，对着**宽度序列**判断，而不是对着颜色判断：

| 形状 | 剪影应长成 |
|---|---|
| 蝙蝠 | 腰鼓（窄→宽→窄）；出现连续 6~8 行满幅 = 已退化成「锚 / 砖」 |
| 史莱姆 | 上窄下宽的圆顶，底部两行压暗 |
| 石头人 | 全直角，肩比腰宽 |
| 龙 | 四个记号分居四个方向（角在上 / 头在左 / 翼在右上 / 尾在右） |

好处是颜色、光照、缩放都不影响判断 —— **只有形状在说话**，
而这恰好是这一轮唯一要改的东西。

### 13.7 生产端断言（`build-assets.py`）

| 断言 | 抓什么 |
|---|---|
| 形状函数 `_put`/`_ell`/`_sym` 坐标钳制 | 越界坐标被安全钳制，不会崩；32 网格下详查逐行宽度避免退化成「锚 / 砖」 |
| `verify_mon_art` ① | **每种形状的剪影互不相同** —— 抓「画了半天结果都是同一个圆」 |
| `verify_mon_art` ② | **同形状的变体必须逐像素不同** —— 抓「换色没生效 / 两只一模一样」 |
| `verify_mon_art` ③ | 底行必须有像素 —— 底部锚定下帧底留白 = 怪物浮在半空 |
| `verify_mon_art` ④ | 每个形状至少有一只怪在用 —— 抓「画了却忘了接进 `PROC_MONSTERS`」 |
| `MONSTERS` ↔ `PROC_MONSTERS` 双向对账 | 声明 `gen` 的与实际画出来的不一致（两边都是漏网的入口） |

### 13.8 运行时断言（`verify-visual.cjs` 的 A12）

生产端对了，不代表**落屏**是对的 —— 渲染层是「有图集用图集，没有就退回
`icons.ts` 的程序化图形」，一旦退回，这一轮就等于白做，而画面看上去「还好」。

所以新增 `board.__sprites()`：把棋子上**真正落屏**的精灵报出来
（`source.uid` + 帧矩形），与 `MANIFEST.json` 的 `idle` 帧对撞：

| 判据 | 说明 |
|---|---|
| 没有**任何**怪物 `uid === null` | null = 走了程序化兜底 |
| 全部怪物共用同一个 `source` | 怪物只有一张 `monsters.png` |
| 落屏帧矩形 ∈ 该怪的 `idle` 四帧，且 64×64（raster tile） | `idle[0]` 会假红 —— 按相位取帧，见下 |
| 这 6 层里该出现的手绘怪物一只不少 | 抓「改完忘了接进去」 |

> 这条断言第一版是**红的**，而且红得有价值：它报出「第 1 层 `greenSlime`
> 落屏 `{x:391}` 而 MANIFEST 是 `{x:340}`」。真因是渲染层按相位在 `idle`
> 四帧间换帧，所以拍到的不是 `idle[0]` —— **期望值写错了，不是实现错了**。
> 改成「必须是 idle 四帧中的某一帧」后转绿，同时把「确实在用图集」这件事
> 钉得更死（连动画帧都覆盖了）。
> 一条断言第一次红，先怀疑期望值 —— 这次就是。

### 13.9 诚实说明：有 2 只做了却看不到

> ⚠️ 与 §9.1 的 `thief` / `fairy` 同类：**`vampire` 与 `demonKingTrue`
> 在 50 层的地图里没有出场点**，只在图集里备着。造型、颜色都已就绪，缺的是
> 地图数据。所以 A12 按「6 个取证层实际出现的 **11 只**」断言，
> 不硬要求 13 只全部见过；否则这条断言会因为参考源自身的缺口而永远红。

### 13.10 取证截图

| 文件 | 验证点 |
|---|---|
| `V1-怪物-史莱姆与蝙蝠-第1层.png` | 史莱姆（圆顶果冻）与蝙蝠（展开的膜翼） |
| `V2-怪物-石头人与大史莱姆-第14层.png` | 石头人（直角方块）/ 大史莱姆（体量更大） |
| `V3-怪物-大乌贼-第15层.png` | 大乌贼（宽圆头 + 六条外张的腕） |
| `V4-怪物-魔龙-第35层.png` | 魔龙（侧视：角 / 长吻 / 翼 / 尾 / 两腿） |
| `V5-怪物-史莱姆王与吸血蝙蝠-第45层.png` | 史莱姆王（三尖金冠）/ 吸血蝙蝠（獠牙） |
| `V6-怪物-魔王-第50层.png` | 魔王（巨角 + 膜翼），第 50 层星界配色 |

---

## 14. 改完素材之后必须做的事（两件，顺序不能反）

这一节是给「下次改美术」的人（包括我自己）留的操作说明。
本轮用户报了「**新的素材在模拟器中没有生效**」，根因不在素材，在**流水线顺序**。

### 14.1 `assets/atlas/*.png` 是唯一源头，`dist-minigame/assets/` 只是副本

```
assets/raw/**            （只读原始素材）
      │  npm run assets            ← 重建 assets/atlas/*.png + MANIFEST.json
      ▼
assets/atlas/*.png       ← 唯一源头，网页版和构建期断言都读它
      │  npm run build            （网页版 dist/）
      │  npm run build:minigame   （小游戏包 dist-minigame/）
      ▼
dist-minigame/assets/*.png   ← tools/copy-minigame-assets.mjs **拷过去的一份副本**
```

关键：**`npm run assets` 不会刷新 `dist-minigame/assets/`。**

- 只跑 `npm run assets` + `npm run build` → 网页版是新素材，**小游戏包还是旧的**。
- 症状极具欺骗性：IDE 里**无报错**、`atlas.ready = true`、棋盘完整，
  **只是画的是上一版画**。不看对比图根本发现不了。

**所以改完美术的顺序是：**

```bash
npm run assets          # 1. 重建图集与 MANIFEST
npm run build:minigame  # 2. 把新图集拷进小游戏包（这一步不能省）
npm run verify:all      # 3. 四套回归
```

`tools/verify-minigame.cjs` 现在有一条断言逐张比 `assets/atlas/*.png` 与
`dist-minigame/assets/*.png` 的 **SHA-256**（取前 12 位），不一致就报出具体文件名：

```
❌ 包内图集与 assets/atlas 逐字节一致（不是上一版美术）
   —— actors.png 源=c3e574dc83fb 包=c21d72d2a698 | monsters.png 源=… 包=…
      —— 重建过图集就要跑一次 npm run build:minigame
```

用**内容哈希而不是时间戳**：时间戳会被 checkout / 复制 / 打包抹掉，
而「字节一致」才是「模拟器里看到的就是我刚画的那份」的准确表述。
细节与完整推演见 `docs/wechat-minigame.md` §9.12。

### 14.2 0x72 包里**只有人形**，别指望它兜住非人形怪物

这一点决定了「什么时候必须自己画」。0x72 地牢包的 24 张角色底图
（`skelet` / `knight` / `imp` / `swampy` / `chort` / `ogre` …）**全是人形** ——
它没有蝙蝠、没有龙、没有乌贼、没有石头人、没有史莱姆。

所以第一版为了「每个名字都有图」只能硬凑，结果就是 §13 里那些
「名字叫蝙蝠、画出来像骑士」的问题。**换素材解决不了**，只能按名称手绘。

| 结论 | 适用范围 |
|---|---|
| 0x72 可直接取用 | 全部地形、10 项道具、**人形**怪物（骷髅 / 骑士 / 小恶魔…） |
| 必须自己画 | 非人形怪物（13 只）、**全部 NPC（6 人 × 4 向 × 4 帧）**、部分道具 |

> 顺带一句：`hue_shift` 调色板变体只适合**单色、同族**的生物换色，
> 不适合「把骑士变成蝙蝠」——形状不对，颜色再对也不对。

