# 本项目源码统一 LF，避免跨平台 diff 噪声
* text=auto eol=lf

# ⚠️ 第三方归档必须逐字节保持原样，禁止任何行尾归一化。
# reference/mota50/ATTRIBUTION.md 用「字节数与上游一致（如 mota50-role.js = 4727）」
# 作为归档忠实度的证据 —— 一旦做了 CRLF→LF 转换，这条校验就不成立了。
reference/** -text
assets/raw/** -text

# 图片一律按二进制处理，避免行尾转换损坏图集
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary

# 归档的第三方代码不计入仓库语言统计
reference/mota50/source/* linguist-vendored
reference/mota50/LICENSE/* linguist-vendored
assets/raw/** linguist-vendored

# 构建产出不计入语言统计
assets/atlas/*.png linguist-generated
assets/MANIFEST.json linguist-generated
